# Copy to .env.local if your backend isn't on the default port.
# Note: NEXT_PUBLIC_* env vars are baked at build time — restart `npm run dev`
# after changing this file or it will be ignored.
NEXT_PUBLIC_API_BASE=http://127.0.0.1:8001
